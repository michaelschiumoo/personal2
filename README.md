# AI Synthesis Agent

Extractive-only synthesis that produces a traceable executive brief (each claim cites source + sentence index), with deterministic fallbacks so it runs reliably on small datasets.

## Quickstart
python main.py demo --format markdown

## Run on your sources
python main.py run --question "..." --sources sources.json --format markdown








